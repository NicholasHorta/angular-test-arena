import { BannerHideDirective } from './banner-hide.directive';

describe('BannerHideDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerHideDirective();
    expect(directive).toBeTruthy();
  });
});
